{% extends 'base.html.twig' %}

{% block title %}Modification de la facture{% endblock %}

{% block body %}
    <h1>Modification de la facture</h1>

    {{ include('facture/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary" href="{{ path('app_facture_index') }}">Retour à la liste</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
