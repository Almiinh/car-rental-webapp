{{ form_start(form) }}
    <div style="width: 500px;">
        {{ form_row(form.date) }}
        {{ form_row(form.compteur_km, {label: 'Compteur km'}) }}
        {{ form_row(form.montant_ht, {label: 'Montant HT'}) }}
        {{ form_row(form.montant_a_payer, {label: 'Montant à payer'}) }}
    </div>
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
    <br\>
{{ form_end(form) }}
